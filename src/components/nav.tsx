import React from "../assets/logo-text.png";

const nav = () => {
    return (
        <nav className = "bg-white-100">
            <img src={React} alt="Logo" />

        </nav>
    );
};

export default nav;